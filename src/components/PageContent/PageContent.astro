---
//import type { CollectionEntry } from 'astro:content';
//import type { MarkdownLayoutProps } from 'astro';
import type { MarkdownHeading } from 'astro';
import MoreMenu from '../RightSidebar/MoreMenu.astro';
import TableOfContents from '../RightSidebar/TableOfContents';

//type Props = CollectionEntry<'docs'>['data'];

type Props ={
	title: string;
	code:string;
	pubDate:string;
	headings: MarkdownHeading[];
	githubEditUrl: string;
	//heroImage: string;
	setUpDifficulty: string;
	
	
	
	
};

const { title, headings,heroImage, setUpDifficulty, githubEditUrl,pubDate, code} = Astro.props;

---

<article id="article" class="content">
	<section class="main-section">
		<!--<img width={720} height={360} src={heroImage} alt=""/>-->
		<h1 class="title"><u>{title}</u></h1>
		
				<h6>Last updated on : <time>{pubDate}</time></h6>
				<h6>Code Version : {code}</h6>
				<h6>Set-Up Difficulty : {setUpDifficulty} </h6>
				<hr>
		
		<nav class="block sm:hidden">
			<TableOfContents client:media="(max-width: 50em)" headings={headings} />
		</nav>
		<slot />
	</section>
	<nav class="block sm:hidden">
		<MoreMenu editHref={githubEditUrl} />
	</nav>
</article>

<style>
	.content {
		padding: 0;
		max-width: 75ch;
		width: 100%;
		height: 100%;
		display: flex;
		flex-direction: column;
	}

	.content > section {
		margin-bottom: 4rem;
	}

	.block {
		display: block;
	}

	@media (min-width: 50em) {
		.sm\:hidden {
			display: none;
		}
	}
</style>
